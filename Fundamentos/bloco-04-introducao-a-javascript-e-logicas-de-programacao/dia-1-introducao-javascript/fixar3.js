const base = 5; //base retangulo
var height = 8; //altura retangulo
const area = base + height;

console.log(area);

const perimeter = base*2 + height*2; //perímetro retangulo

console.log(perimeter);
